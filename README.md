# fixpointlinux.org

The static landing page for **fixpoint-linux** — a Linux system that is a fixed point.

Served by Caddy on `[REDACTED]` from `[REDACTED]`. This repo is the source of
record; deploy by copying `index.html` to that path (preserving the `caddy:caddy` owner).

- Live: https://fixpointlinux.org
- Orgs: https://github.com/fixpoint-linux
